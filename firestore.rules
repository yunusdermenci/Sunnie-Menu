rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /menus/current {
      allow read: if true;
      allow write: if request.auth != null
        && request.auth.token.email == "sunnietattooart@sunnie-menu.com";
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
